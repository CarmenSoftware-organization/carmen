# Keycloak Development Environment Configuration
# Note: These are development-only credentials and should never be used in production

# Database Configuration
KC_DB_PASSWORD=keycloak_dev_password

# Keycloak Admin Configuration
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin123

# Domain Configuration
KC_HOSTNAME=localhost
KC_FRONTEND_URL=http://localhost:8080

# Redis Configuration
REDIS_PASSWORD=dev_redis_password

# SMTP Configuration (using MailHog for development)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@carmen-erp.local
SMTP_FROM_DISPLAY_NAME=Carmen ERP (Dev)
SMTP_AUTH=false
SMTP_STARTTLS=false
SMTP_SSL=false

# Development-specific settings
KC_LOG_LEVEL=DEBUG
KC_METRICS_ENABLED=true
KC_HEALTH_ENABLED=true

# JVM Configuration for development
JAVA_OPTS_APPEND="-Xms512m -Xmx1024m -XX:MetaspaceSize=96M -XX:MaxMetaspaceSize=256m"